framework:
  default_locale: en
  # The demo ships English + French. Drop a locale here (and its translations
  # file) if you don't need it, or add more by dropping messages.<locale>.yaml.
  enabled_locales: ['en', 'fr']
  translator:
    default_path: '%kernel.project_dir%/translations'
    fallbacks:
      - en
