# Scaffolding provenance — generated by `cargo generate`, DO NOT edit by hand.
# Used for version tracking (am I behind the template?) and for a future idempotent
# wrapper update. See README, "Mettre a jour le wrapper".

# Template version at generation time (bumped on every template release).
template_version = "1.0.0"

# Answers used at generation (replayable via `cargo generate -d key=value`).
[answers]
product_name = "{{ product_name }}"
identifier   = "{{ identifier }}"
project_name = "{{ project-name }}"
with_app     = {{ with_app }}
